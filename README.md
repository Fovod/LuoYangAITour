# LuoYangAITour
> 启用虚拟环境

### 配置API_KEY

进入`backend/config.py`配置。

### 安装依赖

在`backend`文件夹运行命令：

```
pip install -r requirements.txt
```

### 启动后端

在`backend`文件夹运行命令：

```
uvicorn main:app
```

### 启动前端

在`frontend`文件夹运行命令：

```
npm run dev
```
